import type {
  VoiceResponseResolution,
  VoiceSession,
  VoiceSessionEvent,
  VoiceSessionTurn,
} from '@/lib/types/voice';

export type VoiceSessionRecord = VoiceSession;

export type VoiceSessionTurnRecord = VoiceSessionTurn;

export type VoiceResponseResolutionRecord = VoiceResponseResolution;

export type VoiceSessionEventRecord = VoiceSessionEvent;
