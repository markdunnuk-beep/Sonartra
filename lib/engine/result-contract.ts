import type { CanonicalAssessmentResultPayload } from '@/types/engine';

/**
 * Alias that makes the canonical payload explicit at usage sites.
 */
export type PersistedAssessmentResult = CanonicalAssessmentResultPayload;
