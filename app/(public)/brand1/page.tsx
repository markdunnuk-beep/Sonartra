import { BRAND_ACCENTS, BrandPageContent } from '../brand/brand-page-content';

export default function BrandTealDeepComparisonPage() {
  return <BrandPageContent accent={BRAND_ACCENTS.tealDeep} />;
}
