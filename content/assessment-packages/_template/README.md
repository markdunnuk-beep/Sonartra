# Sonartra Assessment Package Template

This folder contains the blank reusable assessment package template.

Populated assessment packages should be copied into their own folder before authoring or import work begins.

Example:

```text
content/assessment-packages/flow-state/
```

The workbook is an authoring/import artifact. It is not a runtime dependency, and the runtime engine must never read it directly.
